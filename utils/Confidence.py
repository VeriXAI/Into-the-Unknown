def euclidean_mean_distance(set, point, *args):
    return set.euclidean_mean_distance(point, *args)


def halfspace_distance(set, point, *args):
    return set.halfspace_distance(point, *args)


def center_distance(set, point, *args):
    return set.center_distance(point, *args)


def mean_distance(set, point, *args):
    return set.mean_distance(point, *args)
